# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-09T13:59:51.968Z_

## Tech Stack

- **Languages:** JavaScript
- **Frameworks:** Express
- **Databases:** MySQL, SQL (Sequelize)

## Startup

- **Command:** `docker compose up -d --build (after creating vars.env and patching docker-compose.yml to use Dockerfile instead of Dockerfile-dev, add platform: linux/amd64 to mysql-db, and remove the bind-mount volume on app service)`
- **Port:** 9090
- **Health check path:** `/login`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** yes
- **Auth object ID:** `fHbseB7ViyGQsM2ZiZDSEe`
- **Login:** `POST /register` (form)
- _hint:_ [scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change
- _hint:_ [auth-detection] session auth uses POST /login with form body username=testuser&password=Test1234!. Token location=cookie, field/header=Authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /login Content-Type: application/x-www-form-urlencoded Body: username=brighttest&password=BrightTest123! email=bright@test.brightsec.com, password=BrightTest123!, username=brighttest The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/x-www-form-urlencoded"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.

## Hints

**startup**
- DVNA (Damn Vulnerable NodeJS App) repo: docker-compose.yml uses mysql:5.7 (no arm64 image) - add `platform: linux/amd64` under mysql-db service for Apple Silicon/arm64 hosts. Also Dockerfile-dev + entrypoint-dev.sh use nodemon globally installed under node:carbon (node 8), which is incompatible with nodemon's minimatch dependency (syntax error). Use the production Dockerfile (not Dockerfile-dev) instead - change docker-compose.yml app service to `dockerfile: Dockerfile` and remove the `volumes: .:/app` bind mount (else it overwrites installed node_modules with host's, causing missing deps). Also need to create vars.env (gitignored, not in repo) with: MYSQL_USER=dvna, MYSQL_DATABASE=dvna, MYSQL_PASSWORD=passw0rd, MYSQL_RANDOM_ROOT_PASSWORD=yes, MYSQL_HOST=mysql-db, MYSQL_PORT=3306. App listens on 0.0.0.0:9090, root path "/" redirects (302) to /login which returns 200 OK HTML.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-09T13:59:51.968Z",
  "techStack": {
    "languages": [
      "JavaScript"
    ],
    "frameworks": [
      "Express"
    ],
    "databases": [
      "MySQL",
      "SQL (Sequelize)"
    ]
  },
  "startup": {
    "command": "docker compose up -d --build (after creating vars.env and patching docker-compose.yml to use Dockerfile instead of Dockerfile-dev, add platform: linux/amd64 to mysql-db, and remove the bind-mount volume on app service)",
    "port": 9090,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/login"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "fHbseB7ViyGQsM2ZiZDSEe",
    "registration": {
      "baseUrl": "http://localhost:9090",
      "endpoint": "/register",
      "method": "POST",
      "body": "name=Test User&username=testuser&email=testuser@example.com&password=Test1234!&cpassword=Test1234!",
      "contentType": "form"
    },
    "hints": [
      "[scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change",
      "[auth-detection] session auth uses POST /login with form body username=testuser&password=Test1234!. Token location=cookie, field/header=Authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /login Content-Type: application/x-www-form-urlencoded Body: username=brighttest&password=BrightTest123! email=bright@test.brightsec.com, password=BrightTest123!, username=brighttest The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/x-www-form-urlencoded\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON."
    ],
    "authObjectJson": {
      "otp": null,
      "id": "fHbseB7ViyGQsM2ZiZDSEe",
      "updatedAt": "2026-07-09T13:48:16.981Z",
      "createdAt": "2026-07-09T13:48:16.981Z",
      "repeaterRequired": true,
      "name": "Engine Auth — session",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:9090/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/x-www-form-urlencoded",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "username=brighttest&password=BrightTest123!",
                "bodyType": "clear_text",
                "followRedirects": false,
                "changeMethodOnRedirect": false,
                "maxRedirects": 0
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201,
                    302
                  ]
                }
              ]
            }
          ],
          "embedders": []
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "name": "Location",
          "type": "TRIGGER",
          "location": "header",
          "patterns": [
            "login|signin|sign_in|auth"
          ]
        },
        {
          "type": "OR"
        },
        {
          "type": "TRIGGER",
          "location": "body",
          "patterns": [
            "action=[\"'][^\"']*/login[\"']"
          ]
        }
      ],
      "projectId": "ckVBa7J9gjoEudrwZy3nHJ",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:9090/app/admin",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text",
          "followRedirects": true
        },
        "repeaterId": "qc9qAvBT9EwMzEAd3nu45w"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "DVNA (Damn Vulnerable NodeJS App) repo: docker-compose.yml uses mysql:5.7 (no arm64 image) - add `platform: linux/amd64` under mysql-db service for Apple Silicon/arm64 hosts. Also Dockerfile-dev + entrypoint-dev.sh use nodemon globally installed under node:carbon (node 8), which is incompatible with nodemon's minimatch dependency (syntax error). Use the production Dockerfile (not Dockerfile-dev) instead - change docker-compose.yml app service to `dockerfile: Dockerfile` and remove the `volumes: .:/app` bind mount (else it overwrites installed node_modules with host's, causing missing deps). Also need to create vars.env (gitignored, not in repo) with: MYSQL_USER=dvna, MYSQL_DATABASE=dvna, MYSQL_PASSWORD=passw0rd, MYSQL_RANDOM_ROOT_PASSWORD=yes, MYSQL_HOST=mysql-db, MYSQL_PORT=3306. App listens on 0.0.0.0:9090, root path \"/\" redirects (302) to /login which returns 200 OK HTML."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
