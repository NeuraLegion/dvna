# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-09T15:01:08.482Z_

## Tech Stack

- **Languages:** JavaScript
- **Frameworks:** Express
- **Databases:** MySQL, SQL (Sequelize)

## Startup

- **Command:** `docker compose up -d --build (after creating vars.env, editing docker-compose.yml to build from Dockerfile instead of Dockerfile-dev, and removing the host bind-mount volume)`
- **Port:** 9090
- **Health check path:** `/login`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** yes
- **Auth object ID:** `o63SYSBfKUBLvsxJAQe3pu`
- **Login:** `POST /register` (form)
- _hint:_ [scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change
- _hint:_ [auth-detection] session auth uses POST /login with form body username=testuser&password=Test1234!. Token location=cookie, field/header=Authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /login Content-Type: application/x-www-form-urlencoded Body: username=bright&password=BrightTest123! email=bright@test.brightsec.com, password=BrightTest123!, username=bright The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/x-www-form-urlencoded"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-url] Verified Bright auth validation URL is http://localhost:9090/app/admin/usersapi. Do not mutate or re-encode it into a different identity. Evidence: Unauthenticated GET /app/admin/usersapi returns 302 redirect to /login (confirmed via manual probe with no cookie: HTTP 302, Location: /login). After logging in via POST /login with username=bright&password=BrightTest123! (received 302 redirect to /learn and a connect.sid session cookie), the same GET /app/admin/usersapi with the session cookie returns HTTP 200 with JSON body {"success":true,"users":[...]} listing user records (id, name, login, email, hashed password, role). This is a clear behavioral difference: unauthenticated access is blocked/redirected while authenticated access returns actual protected JSON data, distinct from the login-redirect HTML body. Route is defined in routes/app.js as router.get('/admin/usersapi'

## Hints

**startup**
- DVNA (Damn Vulnerable NodeJS App) repo: use docker-compose.yml with vars.env file (create if missing) containing MYSQL_USER=dvna, MYSQL_DATABASE=dvna, MYSQL_PASSWORD=passw0rd, MYSQL_ROOT_PASSWORD=rootpassw0rd, MYSQL_HOST=mysql-db, MYSQL_PORT=3306, APP_PORT=9090. The default docker-compose.yml uses Dockerfile-dev + nodemon + bind-mounts host ./:/app, which on arm64/host mounts break because node_modules built in image get overwritten by host bind mount (missing/incompatible), and nodemon in that image is broken (SyntaxError from minimatch incompatible with old node:carbon i.e. Node 8). Fix: change docker-compose.yml service "app" to build using dockerfile: Dockerfile (production, not Dockerfile-dev) and REMOVE the "volumes: - .:/app" bind mount line. Then `docker compose up -d --build` works: app listens on 0.0.0.0:9090, connects to mysql-db, creates tables, logs "It worked!". Verified GE

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-09T15:01:08.482Z",
  "techStack": {
    "languages": [
      "JavaScript"
    ],
    "frameworks": [
      "Express"
    ],
    "databases": [
      "MySQL",
      "SQL (Sequelize)"
    ]
  },
  "startup": {
    "command": "docker compose up -d --build (after creating vars.env, editing docker-compose.yml to build from Dockerfile instead of Dockerfile-dev, and removing the host bind-mount volume)",
    "port": 9090,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/login"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "o63SYSBfKUBLvsxJAQe3pu",
    "registration": {
      "baseUrl": "http://localhost:9090",
      "endpoint": "/register",
      "method": "POST",
      "body": "name=Test User&username=testuser&email=testuser@example.com&password=Test1234!&cpassword=Test1234!",
      "contentType": "form"
    },
    "hints": [
      "[scan-prep-warning] Scan-prep reported success without applying or documenting any rate-limit/security-control change",
      "[auth-detection] session auth uses POST /login with form body username=testuser&password=Test1234!. Token location=cookie, field/header=Authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /login Content-Type: application/x-www-form-urlencoded Body: username=bright&password=BrightTest123! email=bright@test.brightsec.com, password=BrightTest123!, username=bright The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/x-www-form-urlencoded\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-url] Verified Bright auth validation URL is http://localhost:9090/app/admin/usersapi. Do not mutate or re-encode it into a different identity. Evidence: Unauthenticated GET /app/admin/usersapi returns 302 redirect to /login (confirmed via manual probe with no cookie: HTTP 302, Location: /login). After logging in via POST /login with username=bright&password=BrightTest123! (received 302 redirect to /learn and a connect.sid session cookie), the same GET /app/admin/usersapi with the session cookie returns HTTP 200 with JSON body {\"success\":true,\"users\":[...]} listing user records (id, name, login, email, hashed password, role). This is a clear behavioral difference: unauthenticated access is blocked/redirected while authenticated access returns actual protected JSON data, distinct from the login-redirect HTML body. Route is defined in routes/app.js as router.get('/admin/usersapi'"
    ],
    "authObjectJson": {
      "otp": null,
      "id": "o63SYSBfKUBLvsxJAQe3pu",
      "updatedAt": "2026-07-09T14:50:34.217Z",
      "createdAt": "2026-07-09T14:50:34.216Z",
      "repeaterRequired": true,
      "name": "Engine Auth — session",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:9090/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/x-www-form-urlencoded",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "username=bright&password=BrightTest123!",
                "bodyType": "clear_text",
                "followRedirects": false,
                "changeMethodOnRedirect": false,
                "maxRedirects": 0
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201,
                    302
                  ]
                }
              ]
            }
          ],
          "embedders": []
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        },
        {
          "type": "OR"
        },
        {
          "name": "Location",
          "type": "TRIGGER",
          "location": "header",
          "patterns": [
            "login|signin|sign_in|auth"
          ]
        },
        {
          "type": "OR"
        },
        {
          "type": "TRIGGER",
          "location": "body",
          "patterns": [
            "action=[\"'][^\"']*/login[\"']"
          ]
        }
      ],
      "projectId": "ckVBa7J9gjoEudrwZy3nHJ",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:9090/app/admin/usersapi",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text",
          "followRedirects": true
        },
        "repeaterId": "fTy4k4Dsww5v3PfMdxNbeR"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "DVNA (Damn Vulnerable NodeJS App) repo: use docker-compose.yml with vars.env file (create if missing) containing MYSQL_USER=dvna, MYSQL_DATABASE=dvna, MYSQL_PASSWORD=passw0rd, MYSQL_ROOT_PASSWORD=rootpassw0rd, MYSQL_HOST=mysql-db, MYSQL_PORT=3306, APP_PORT=9090. The default docker-compose.yml uses Dockerfile-dev + nodemon + bind-mounts host ./:/app, which on arm64/host mounts break because node_modules built in image get overwritten by host bind mount (missing/incompatible), and nodemon in that image is broken (SyntaxError from minimatch incompatible with old node:carbon i.e. Node 8). Fix: change docker-compose.yml service \"app\" to build using dockerfile: Dockerfile (production, not Dockerfile-dev) and REMOVE the \"volumes: - .:/app\" bind mount line. Then `docker compose up -d --build` works: app listens on 0.0.0.0:9090, connects to mysql-db, creates tables, logs \"It worked!\". Verified GE"
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
