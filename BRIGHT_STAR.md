# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-08T14:07:49.574Z_

## Tech Stack

- **Languages:** JavaScript
- **Frameworks:** Express
- **Databases:** MySQL, SQL (Sequelize)

## Startup

- **Command:** `docker compose up -d --build`
- **Port:** 9090
- **Health check path:** `/login`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** yes
- **Auth object ID:** `fSJsNWGWtgpm6m5jn9Sj99`
- **Login:** `POST /register` (form)
- **Seed commands:**
  - [host] `docker ps --format '{{.ID}} {{.Names}} {{.Image}}'`
  - [docker:dvna-app-1] `printenv | sort | grep MYSQL`
  - [docker:dvna-app-1] `node -e "const db=require('./models'); db.User.findAll({attributes:['id','name','login','email','role']}).then(u=>{console.log(JSON.stringify(u,null,2)); process.exit(0)}).catch(e=>{console.error(e); process.exit(1)})"`
  - [docker:dvna-app-1] `node -e "const db=require('./models'); db.User.findOne({where:{login:'bright_test'}}).then(u=>{console.log(u?JSON.stringify(u.toJSON(),null,2):'NOTFOUND'); process.exit(0)}).catch(e=>{console.error(e); process.exit(1)})"`
  - [docker:dvna-mysql-db-1] `mysql -udvna -ppassw0rd dvna -e "select id,name,login,email,role from Users;"`
  - [docker:dvna-app-1] `node -e "const db=require('./models'); db.User.create({name:'Bright Test', login:'bright_test', email:'bright@test.com', password: require('bcrypt').hashSync('BrightTest123!', require('bcrypt').genSaltSync(10), null), role:'admin'}).then(u=>{console.log(JSON.stringify({id:u.id,login:u.login,email:u.email,role:u.role})) ; process.exit(0)}).catch(e=>{console.error(e); process.exit(1)})"`
  - [docker:dvna-app-1] `node -e "const db=require('./models'); db.User.findAll({where:{login:'bright_test'},attributes:['id','name','login','email','role']}).then(u=>{console.log(JSON.stringify(u,null,2)); process.exit(0)}).catch(e=>{console.error(e); process.exit(1)})"`
  - [docker:dvna-app-1] `node - <<'NODE'
const passport = require('passport');
require('./core/passport')(passport);
const db = require('./models');
const req = { body:{}, flash:()=>{} };
passport._strategies['login']._verify(req,'bright_test','BrightTest123!',(err,user,info)=>{
  console.log(JSON.stringify({err:err?String(err):null,user:user?{id:user.id,login:user.login,email:user.email,role:user.role}:null,info:info||null}));
  process.exit(0);
});
NODE`
  - [docker:dvna-mysql-db-1] `mysql -udvna -ppassw0rd dvna -e "select id,name,login,email,role from Users where login='bright_test';"`
- _hint:_ [scan-prep] DAST blockers were reviewed and no active rate limiter was present; session lifetime was relaxed, the app was restarted, and rapid POST verification confirmed login requests are not being throttled.
- _hint:_ [scan-prep] Inspected the Express/Passport/MySQL app source and runtime container state for rate limiting, throttling, CAPTCHA, CSRF, lockout, IP filtering, and other scanner-blocking controls.
- _hint:_ [scan-prep] Queried the running app container and application data to confirm the live auth path and seeded user account (`dastuser`) used for verification.
- _hint:_ [scan-prep] Confirmed there is no code-level or runtime rate limiter, account lockout, CAPTCHA, CSRF enforcement middleware, WAF/filtering, or IP allow/block logic active in this app.
- _hint:_ [scan-prep] Relaxed session handling in `server.js` by enabling rolling sessions and extending cookie lifetime to 7 days (`maxAge`) so aggressive scanner activity is less likely to lose auth due to session expiry.
- _hint:_ [scan-prep] Restarted the app container to apply the source change.
- _hint:_ [scan-prep] Verified with 6 rapid POST requests to the real login endpoint `/login` using an existing username; all requests returned normal auth failure redirects (302 to `/login`) and no 429/throttle/rate-limit response was observed.
- _hint:_ [auth-detection] session auth uses POST /login with form body username=testuser_dast&password=Passw0rd!23. Token location=cookie, field/header=Authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-seed] Seeded credentials are username=bright_test, email=bright@test.com, password=BrightTest123!. Preserve the app's detected login field names in the login body: username=bright_test&password=BrightTest123%21.
- _hint:_ Verified seeded admin credentials work on POST /login with form body username=bright_test&password=BrightTest123%21, returning 302 Location /learn and setting connect.sid. Unauthenticated GET /app/admin/usersapi redirects 302 to /login; authenticated GET /app/admin/usersapi returns 200 JSON with users list, making it a valid session test URL.

## Hints

**startup**
- DVNA app uses docker-compose.yml with services app and mysql-db; app publishes host port 9090 and listens on 0.0.0.0:9090 via config/server.js.
- App requires MySQL env vars via vars.env: MYSQL_USER, MYSQL_DATABASE, MYSQL_PASSWORD, MYSQL_RANDOM_ROOT_PASSWORD, plus MYSQL_HOST/MYSQL_PORT for non-dev entrypoint; compose dev entrypoint waits for mysql-db:3306 then runs startup.sh.

**infra**
- On arm64 hosts, docker-compose fails because mysql:5.7 has no linux/arm64 manifest; use mysql/mysql-server:5.7 or another arm64-capable MySQL 5.7-compatible image for mysql-db.
- Startup failed with node:carbon because Dockerfile-dev installs latest nodemon 3.x, which is incompatible with Node 8 and crashes with SyntaxError in minimatch; pin nodemon to an older Node-8-compatible version or avoid nodemon.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-08T14:07:49.574Z",
  "techStack": {
    "languages": [
      "JavaScript"
    ],
    "frameworks": [
      "Express"
    ],
    "databases": [
      "MySQL",
      "SQL (Sequelize)"
    ]
  },
  "startup": {
    "command": "docker compose up -d --build",
    "port": 9090,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/login"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "fSJsNWGWtgpm6m5jn9Sj99",
    "registration": {
      "baseUrl": "http://localhost:9090",
      "endpoint": "/register",
      "method": "POST",
      "body": "name=Test+User&username=testuser_dast&email=testuser_dast%40example.com&password=Passw0rd!23&cpassword=Passw0rd!23",
      "contentType": "form"
    },
    "seedCommands": [
      {
        "type": "host",
        "command": "docker ps --format '{{.ID}} {{.Names}} {{.Image}}'"
      },
      {
        "type": "docker",
        "command": "printenv | sort | grep MYSQL",
        "container": "dvna-app-1"
      },
      {
        "type": "docker",
        "command": "node -e \"const db=require('./models'); db.User.findAll({attributes:['id','name','login','email','role']}).then(u=>{console.log(JSON.stringify(u,null,2)); process.exit(0)}).catch(e=>{console.error(e); process.exit(1)})\"",
        "container": "dvna-app-1"
      },
      {
        "type": "docker",
        "command": "node -e \"const db=require('./models'); db.User.findOne({where:{login:'bright_test'}}).then(u=>{console.log(u?JSON.stringify(u.toJSON(),null,2):'NOTFOUND'); process.exit(0)}).catch(e=>{console.error(e); process.exit(1)})\"",
        "container": "dvna-app-1"
      },
      {
        "type": "docker",
        "command": "mysql -udvna -ppassw0rd dvna -e \"select id,name,login,email,role from Users;\"",
        "container": "dvna-mysql-db-1"
      },
      {
        "type": "docker",
        "command": "node -e \"const db=require('./models'); db.User.create({name:'Bright Test', login:'bright_test', email:'bright@test.com', password: require('bcrypt').hashSync('BrightTest123!', require('bcrypt').genSaltSync(10), null), role:'admin'}).then(u=>{console.log(JSON.stringify({id:u.id,login:u.login,email:u.email,role:u.role})) ; process.exit(0)}).catch(e=>{console.error(e); process.exit(1)})\"",
        "container": "dvna-app-1"
      },
      {
        "type": "docker",
        "command": "node -e \"const db=require('./models'); db.User.findAll({where:{login:'bright_test'},attributes:['id','name','login','email','role']}).then(u=>{console.log(JSON.stringify(u,null,2)); process.exit(0)}).catch(e=>{console.error(e); process.exit(1)})\"",
        "container": "dvna-app-1"
      },
      {
        "type": "docker",
        "command": "node - <<'NODE'\nconst passport = require('passport');\nrequire('./core/passport')(passport);\nconst db = require('./models');\nconst req = { body:{}, flash:()=>{} };\npassport._strategies['login']._verify(req,'bright_test','BrightTest123!',(err,user,info)=>{\n  console.log(JSON.stringify({err:err?String(err):null,user:user?{id:user.id,login:user.login,email:user.email,role:user.role}:null,info:info||null}));\n  process.exit(0);\n});\nNODE",
        "container": "dvna-app-1"
      },
      {
        "type": "docker",
        "command": "mysql -udvna -ppassw0rd dvna -e \"select id,name,login,email,role from Users where login='bright_test';\"",
        "container": "dvna-mysql-db-1"
      }
    ],
    "hints": [
      "[scan-prep] DAST blockers were reviewed and no active rate limiter was present; session lifetime was relaxed, the app was restarted, and rapid POST verification confirmed login requests are not being throttled.",
      "[scan-prep] Inspected the Express/Passport/MySQL app source and runtime container state for rate limiting, throttling, CAPTCHA, CSRF, lockout, IP filtering, and other scanner-blocking controls.",
      "[scan-prep] Queried the running app container and application data to confirm the live auth path and seeded user account (`dastuser`) used for verification.",
      "[scan-prep] Confirmed there is no code-level or runtime rate limiter, account lockout, CAPTCHA, CSRF enforcement middleware, WAF/filtering, or IP allow/block logic active in this app.",
      "[scan-prep] Relaxed session handling in `server.js` by enabling rolling sessions and extending cookie lifetime to 7 days (`maxAge`) so aggressive scanner activity is less likely to lose auth due to session expiry.",
      "[scan-prep] Restarted the app container to apply the source change.",
      "[scan-prep] Verified with 6 rapid POST requests to the real login endpoint `/login` using an existing username; all requests returned normal auth failure redirects (302 to `/login`) and no 429/throttle/rate-limit response was observed.",
      "[auth-detection] session auth uses POST /login with form body username=testuser_dast&password=Passw0rd!23. Token location=cookie, field/header=Authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-seed] Seeded credentials are username=bright_test, email=bright@test.com, password=BrightTest123!. Preserve the app's detected login field names in the login body: username=bright_test&password=BrightTest123%21.",
      "Verified seeded admin credentials work on POST /login with form body username=bright_test&password=BrightTest123%21, returning 302 Location /learn and setting connect.sid. Unauthenticated GET /app/admin/usersapi redirects 302 to /login; authenticated GET /app/admin/usersapi returns 200 JSON with users list, making it a valid session test URL."
    ],
    "authObjectJson": {
      "otp": null,
      "id": "fSJsNWGWtgpm6m5jn9Sj99",
      "updatedAt": "2026-07-08T13:31:11.840Z",
      "createdAt": "2026-07-08T13:31:11.840Z",
      "repeaterRequired": true,
      "name": "Engine Auth — session",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:9090/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/x-www-form-urlencoded",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "username=bright_test&password=BrightTest123%21",
                "bodyType": "clear_text",
                "followRedirects": false,
                "changeMethodOnRedirect": false,
                "maxRedirects": 0
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201,
                    302
                  ]
                }
              ]
            }
          ],
          "embedders": []
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "name": "Location",
          "type": "TRIGGER",
          "location": "header",
          "patterns": [
            "login|signin|sign_in|auth"
          ]
        },
        {
          "type": "OR"
        },
        {
          "type": "TRIGGER",
          "location": "body",
          "patterns": [
            "action=[\"'][^\"']*/login[\"']"
          ]
        }
      ],
      "projectId": "ckVBa7J9gjoEudrwZy3nHJ",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:9090/app/admin/usersapi",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text",
          "followRedirects": true
        },
        "repeaterId": "nPwADhNjKJmfQsVQ615ARD"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "DVNA app uses docker-compose.yml with services app and mysql-db; app publishes host port 9090 and listens on 0.0.0.0:9090 via config/server.js.",
      "App requires MySQL env vars via vars.env: MYSQL_USER, MYSQL_DATABASE, MYSQL_PASSWORD, MYSQL_RANDOM_ROOT_PASSWORD, plus MYSQL_HOST/MYSQL_PORT for non-dev entrypoint; compose dev entrypoint waits for mysql-db:3306 then runs startup.sh."
    ],
    "infra": [
      "On arm64 hosts, docker-compose fails because mysql:5.7 has no linux/arm64 manifest; use mysql/mysql-server:5.7 or another arm64-capable MySQL 5.7-compatible image for mysql-db.",
      "Startup failed with node:carbon because Dockerfile-dev installs latest nodemon 3.x, which is incompatible with Node 8 and crashes with SyntaxError in minimatch; pin nodemon to an older Node-8-compatible version or avoid nodemon."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
