# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-10T11:19:06.003Z_

## Tech Stack

- **Languages:** JavaScript
- **Frameworks:** Express
- **Databases:** MySQL, SQL (Sequelize)

## Startup

- **Command:** `docker compose up -d --build`
- **Port:** 9090

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** yes
- **Auth object ID:** `w64jkKRkxjQDyRF4xNLYW9`
- **Login:** `POST /register` (form)
- _hint:_ [scan-prep] No active rate limiter or CSRF/CAPTCHA blocker was present; session lifetime was extended and rapid login POST verification confirmed the app is permissive for DAST scanning.
- _hint:_ [scan-prep] Inspected the Express/Passport/Sequelize codebase and running containers for rate limiting, lockout, CAPTCHA, CSRF enforcement, and other scanner-blocking controls; found no code-level or runtime rate limiter, lockout, CAPTCHA, WAF, or CSRF middleware enabled in this app.
- _hint:_ [scan-prep] Queried the live MySQL-backed application data to confirm there were no database-backed runtime settings implementing hidden rate/throttle controls; this app does not use framework-level rate-limit settings storage.
- _hint:_ [scan-prep] Relaxed session timeout behavior in server.js by setting the express-session cookie maxAge to 31536000000 (1 year) so aggressive session expiry will not interfere with DAST workflows.
- _hint:_ [scan-prep] Restarted the app container to apply the source change and verified with 5 rapid POST requests to the real /login endpoint using an existing account name; all requests returned normal auth failure redirects (302 to /login) with no 429/throttle/too-many-requests behavior.
- _hint:_ [auth-detection] session auth uses POST /login with form body username=dastuser&password=Passw0rd!&name=DAST%20User&email=dastuser%40example.com&cpassword=Passw0rd!. Token location=cookie, field/header=Authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /login Content-Type: application/x-www-form-urlencoded Body: username=bright@test.brightsec.com&password=BrightTest123! email=bright@test.brightsec.com, password=BrightTest123!, username=bright@test.brightsec.com The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/x-www-form-urlencoded"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.

## Hints

**startup**
- Repo is DVNA (Express + Sequelize + MySQL). Existing docker-compose.yml starts app on host port 9090 and mysql-db on internal 3306 using vars.env with MYSQL_USER, MYSQL_DATABASE, MYSQL_PASSWORD, MYSQL_HOST=mysql-db, MYSQL_PORT=3306.

**infra**
- On arm64 hosts, DVNA's app container using node:carbon compiles native modules (bcrypt, libxmljs) on first boot because startup.sh runs npm install from bind-mounted source; app may take over a minute before listening on 9090.
- Attempt 1 failed because Dockerfile-dev used node:carbon and installed latest nodemon globally; after npm install, nodemon crashed on Node 8 syntax in minimatch. Fix: switch dev image to newer Node and pin nodemon, plus install build tools for native modules.
- Attempt 2 with Node 18 still crashed because bind-mounting the repo over /app reused host-created node_modules built for Node 8; add an anonymous volume at /app/node_modules in compose so container installs modules matching the current image runtime.
- Attempt 3 showed bcrypt@1.0.3 cannot compile on Node 18 and then app lacked dependencies. Better compatibility path is older Node runtime matching native modules, but on current Docker Hub use a Debian-based Node 8 image (e.g. node:8-buster) and an older nodemon compatible with Node 8.
- Attempt 4 build failed because node:8-buster uses archived Debian repos. Fix by rewriting apt sources to archive.debian.org and disabling Valid-Until checks before apt-get update.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-10T11:19:06.003Z",
  "techStack": {
    "languages": [
      "JavaScript"
    ],
    "frameworks": [
      "Express"
    ],
    "databases": [
      "MySQL",
      "SQL (Sequelize)"
    ]
  },
  "startup": {
    "command": "docker compose up -d --build",
    "port": 9090,
    "prerequisites": [],
    "envVars": {}
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "w64jkKRkxjQDyRF4xNLYW9",
    "registration": {
      "baseUrl": "http://localhost:9090",
      "endpoint": "/register",
      "method": "POST",
      "body": "name=DAST%20User&username=dastuser&email=dastuser%40example.com&password=Passw0rd!&cpassword=Passw0rd!",
      "contentType": "form"
    },
    "hints": [
      "[scan-prep] No active rate limiter or CSRF/CAPTCHA blocker was present; session lifetime was extended and rapid login POST verification confirmed the app is permissive for DAST scanning.",
      "[scan-prep] Inspected the Express/Passport/Sequelize codebase and running containers for rate limiting, lockout, CAPTCHA, CSRF enforcement, and other scanner-blocking controls; found no code-level or runtime rate limiter, lockout, CAPTCHA, WAF, or CSRF middleware enabled in this app.",
      "[scan-prep] Queried the live MySQL-backed application data to confirm there were no database-backed runtime settings implementing hidden rate/throttle controls; this app does not use framework-level rate-limit settings storage.",
      "[scan-prep] Relaxed session timeout behavior in server.js by setting the express-session cookie maxAge to 31536000000 (1 year) so aggressive session expiry will not interfere with DAST workflows.",
      "[scan-prep] Restarted the app container to apply the source change and verified with 5 rapid POST requests to the real /login endpoint using an existing account name; all requests returned normal auth failure redirects (302 to /login) with no 429/throttle/too-many-requests behavior.",
      "[auth-detection] session auth uses POST /login with form body username=dastuser&password=Passw0rd!&name=DAST%20User&email=dastuser%40example.com&cpassword=Passw0rd!. Token location=cookie, field/header=Authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /login Content-Type: application/x-www-form-urlencoded Body: username=bright@test.brightsec.com&password=BrightTest123! email=bright@test.brightsec.com, password=BrightTest123!, username=bright@test.brightsec.com The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/x-www-form-urlencoded\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON."
    ],
    "authObjectJson": {
      "otp": null,
      "id": "w64jkKRkxjQDyRF4xNLYW9",
      "updatedAt": "2026-07-10T11:10:02.704Z",
      "createdAt": "2026-07-10T11:10:02.704Z",
      "repeaterRequired": true,
      "name": "Engine Auth — session",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:9090/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/x-www-form-urlencoded",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "username=bright@test.brightsec.com&password=BrightTest123!",
                "bodyType": "clear_text",
                "followRedirects": false,
                "changeMethodOnRedirect": false,
                "maxRedirects": 0
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201,
                    302
                  ]
                }
              ]
            }
          ],
          "embedders": []
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "name": "Location",
          "type": "TRIGGER",
          "location": "header",
          "patterns": [
            "login|signin|sign_in|auth"
          ]
        },
        {
          "type": "OR"
        },
        {
          "type": "TRIGGER",
          "location": "body",
          "patterns": [
            "action=[\"'][^\"']*/login[\"']"
          ]
        }
      ],
      "projectId": "ckVBa7J9gjoEudrwZy3nHJ",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:9090/app/admin/usersapi",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text",
          "followRedirects": true
        },
        "repeaterId": "w7KYVibScQ9w3GjxQpe5Y4"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "Repo is DVNA (Express + Sequelize + MySQL). Existing docker-compose.yml starts app on host port 9090 and mysql-db on internal 3306 using vars.env with MYSQL_USER, MYSQL_DATABASE, MYSQL_PASSWORD, MYSQL_HOST=mysql-db, MYSQL_PORT=3306."
    ],
    "infra": [
      "On arm64 hosts, DVNA's app container using node:carbon compiles native modules (bcrypt, libxmljs) on first boot because startup.sh runs npm install from bind-mounted source; app may take over a minute before listening on 9090.",
      "Attempt 1 failed because Dockerfile-dev used node:carbon and installed latest nodemon globally; after npm install, nodemon crashed on Node 8 syntax in minimatch. Fix: switch dev image to newer Node and pin nodemon, plus install build tools for native modules.",
      "Attempt 2 with Node 18 still crashed because bind-mounting the repo over /app reused host-created node_modules built for Node 8; add an anonymous volume at /app/node_modules in compose so container installs modules matching the current image runtime.",
      "Attempt 3 showed bcrypt@1.0.3 cannot compile on Node 18 and then app lacked dependencies. Better compatibility path is older Node runtime matching native modules, but on current Docker Hub use a Debian-based Node 8 image (e.g. node:8-buster) and an older nodemon compatible with Node 8.",
      "Attempt 4 build failed because node:8-buster uses archived Debian repos. Fix by rewriting apt sources to archive.debian.org and disabling Valid-Until checks before apt-get update."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
