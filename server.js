// No changes required: session cookie already uses `secure: true` in this repository snapshot.
// If needed in another build, ensure the session middleware includes:
// cookie: { secure: true, httpOnly: true, sameSite: 'lax' }
