# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-10T13:50:59.446Z_

## Tech Stack

- **Languages:** JavaScript
- **Frameworks:** Express
- **Databases:** MySQL, SQL (Sequelize)

## Startup

- **Command:** `docker compose down -v && docker compose up -d --build`
- **Port:** 9090
- **Health check path:** `/login`

## First-Run Setup

- **Completed:** no

## Authentication

- **Has auth:** yes
- **Auth object ID:** `m1ti8UkYoFW5QxvGrCpxmB`
- **Login:** `POST /register` (form)
- _hint:_ [scan-prep] Prepared the app for DAST by confirming no active rate limiting/lockout/CAPTCHA/CSRF enforcement exists, extending session lifetime for scanner stability, restarting the app, and verifying 6 rapid POST logins hit the real auth path without any rate-limit response.
- _hint:_ [scan-prep] Inspected the running Express/Passport/MySQL app and queried runtime state in both app and MySQL containers; confirmed there are no framework- or code-level rate limiters, lockout handlers, CAPTCHA controls, CSRF middleware, IP filters, or WAF-style request guards enabled in this build.
- _hint:_ [scan-prep] Verified the real auth processing path by creating a stable test account (`testdast1`) via `POST /register`, then sending 6 rapid `POST /login` requests with that username and a bad password. All requests consistently returned normal auth failure redirects (`302 /login`) with no `429`, throttle text, or blocking response.
- _hint:_ [scan-prep] Relaxed the only scanner-relevant control present: extended session lifetime in `server.js` by setting the Express session cookie `maxAge` to 31536000000 ms (~1 year), then restarted the app container and re-verified login hammering still worked without throttling.
- _hint:_ [auth-detection] session auth uses POST /login with form body username=dastuser&password=Passw0rd!. Token location=cookie, field/header=Authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /login Content-Type: application/x-www-form-urlencoded Body: username=bright@test.brightsec.com&password=BrightTest123! email=bright@test.brightsec.com, password=BrightTest123!, username=bright@test.brightsec.com The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/x-www-form-urlencoded"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ Protected app routes are mounted under /app via server.js. Verified unauthenticated GETs to /app/, /app/ping, /app/products, /app/admin, /app/admin/usersapi, and /app/usersearch all return 302 with Location: /login, so /app/ping or /app/ are good redirect-based session testUrl candidates.

## Rate Limits / Scan Prep

- [dvna-mysql-db-1] `mysql -udvna -ppassw0rd -D dvna -e "update Users set password='$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy' where login='testdast1'; select login,role from Users where login='testdast1';"`

## Hints

**startup**
- DVNA is an Express app started by `npm start`/`node server.js`, listens on 0.0.0.0 and port 9090 by default via config/server.js, and depends on MySQL env vars MYSQL_USER, MYSQL_PASSWORD, MYSQL_DATABASE, MYSQL_HOST, MYSQL_PORT.
- Repo includes docker-compose.yml with services `app` and `mysql-db`; app builds from Dockerfile-dev, publishes 9090:9090, and expects env file `vars.env`.

**infra**
- Attempt 1 failed because Dockerfile-dev uses node:carbon (Node 8) and installs the latest nodemon globally; modern nodemon/minimatch syntax crashes on Node 8 with `SyntaxError: Unexpected token ;` before the app starts.
- Attempt 2 reached nodemon on Node 20, but bind-mounted host `node_modules`/old artifacts caused native module mismatch: bcrypt binary was built for NODE_MODULE_VERSION 57 and crashed under Node 20 (requires 115).
- Attempt 3 failed because bcrypt@1.0.3 cannot compile on Node 20; after adding anonymous /app/node_modules volume, `npm install` inside startup.sh leaves install incomplete and server crashes with missing modules.
- Replaced native bcrypt dependency with pure-JS bcryptjs in package.json and the three require() sites, to avoid node-gyp/native binary startup failures across Node versions.
- libxmljs was required only by the XML bulkProducts handler in core/appHandler.js; removing that native dependency and stubbing the handler avoids another Node 20 native build blocker.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-10T13:50:59.446Z",
  "techStack": {
    "languages": [
      "JavaScript"
    ],
    "frameworks": [
      "Express"
    ],
    "databases": [
      "MySQL",
      "SQL (Sequelize)"
    ]
  },
  "startup": {
    "command": "docker compose down -v && docker compose up -d --build",
    "port": 9090,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/login"
  },
  "setup": {
    "completed": false
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "m1ti8UkYoFW5QxvGrCpxmB",
    "registration": {
      "baseUrl": "http://localhost:9090",
      "endpoint": "/register",
      "method": "POST",
      "body": "name=DAST%20User&username=dastuser&email=dastuser%40example.com&password=Passw0rd!&cpassword=Passw0rd!",
      "contentType": "form"
    },
    "hints": [
      "[scan-prep] Prepared the app for DAST by confirming no active rate limiting/lockout/CAPTCHA/CSRF enforcement exists, extending session lifetime for scanner stability, restarting the app, and verifying 6 rapid POST logins hit the real auth path without any rate-limit response.",
      "[scan-prep] Inspected the running Express/Passport/MySQL app and queried runtime state in both app and MySQL containers; confirmed there are no framework- or code-level rate limiters, lockout handlers, CAPTCHA controls, CSRF middleware, IP filters, or WAF-style request guards enabled in this build.",
      "[scan-prep] Verified the real auth processing path by creating a stable test account (`testdast1`) via `POST /register`, then sending 6 rapid `POST /login` requests with that username and a bad password. All requests consistently returned normal auth failure redirects (`302 /login`) with no `429`, throttle text, or blocking response.",
      "[scan-prep] Relaxed the only scanner-relevant control present: extended session lifetime in `server.js` by setting the Express session cookie `maxAge` to 31536000000 ms (~1 year), then restarted the app container and re-verified login hammering still worked without throttling.",
      "[auth-detection] session auth uses POST /login with form body username=dastuser&password=Passw0rd!. Token location=cookie, field/header=Authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /login Content-Type: application/x-www-form-urlencoded Body: username=bright@test.brightsec.com&password=BrightTest123! email=bright@test.brightsec.com, password=BrightTest123!, username=bright@test.brightsec.com The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/x-www-form-urlencoded\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "Protected app routes are mounted under /app via server.js. Verified unauthenticated GETs to /app/, /app/ping, /app/products, /app/admin, /app/admin/usersapi, and /app/usersearch all return 302 with Location: /login, so /app/ping or /app/ are good redirect-based session testUrl candidates."
    ],
    "authObjectJson": {
      "otp": null,
      "id": "m1ti8UkYoFW5QxvGrCpxmB",
      "updatedAt": "2026-07-10T12:05:43.176Z",
      "createdAt": "2026-07-10T12:05:43.176Z",
      "repeaterRequired": true,
      "name": "Engine Auth — session",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:9090/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/x-www-form-urlencoded",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "username=bright@test.brightsec.com&password=BrightTest123!",
                "bodyType": "clear_text",
                "followRedirects": false,
                "changeMethodOnRedirect": false,
                "maxRedirects": 0
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201,
                    302
                  ]
                }
              ]
            }
          ],
          "embedders": []
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "name": "Location",
          "type": "TRIGGER",
          "location": "header",
          "patterns": [
            "login|signin|sign_in|auth"
          ]
        },
        {
          "type": "OR"
        },
        {
          "type": "TRIGGER",
          "location": "body",
          "patterns": [
            "action=[\"'][^\"']*/login[\"']"
          ]
        }
      ],
      "projectId": "ckVBa7J9gjoEudrwZy3nHJ",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:9090/app/ping",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text",
          "followRedirects": true
        },
        "repeaterId": "eJQawdFtKYStKr75AbNDSr"
      },
      "staticHeaders": null
    }
  },
  "limits": {
    "scanPrepReplayCommands": [
      {
        "container": "dvna-mysql-db-1",
        "command": "mysql -udvna -ppassw0rd -D dvna -e \"update Users set password='$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy' where login='testdast1'; select login,role from Users where login='testdast1';\""
      }
    ]
  },
  "hints": {
    "startup": [
      "DVNA is an Express app started by `npm start`/`node server.js`, listens on 0.0.0.0 and port 9090 by default via config/server.js, and depends on MySQL env vars MYSQL_USER, MYSQL_PASSWORD, MYSQL_DATABASE, MYSQL_HOST, MYSQL_PORT.",
      "Repo includes docker-compose.yml with services `app` and `mysql-db`; app builds from Dockerfile-dev, publishes 9090:9090, and expects env file `vars.env`."
    ],
    "infra": [
      "Attempt 1 failed because Dockerfile-dev uses node:carbon (Node 8) and installs the latest nodemon globally; modern nodemon/minimatch syntax crashes on Node 8 with `SyntaxError: Unexpected token ;` before the app starts.",
      "Attempt 2 reached nodemon on Node 20, but bind-mounted host `node_modules`/old artifacts caused native module mismatch: bcrypt binary was built for NODE_MODULE_VERSION 57 and crashed under Node 20 (requires 115).",
      "Attempt 3 failed because bcrypt@1.0.3 cannot compile on Node 20; after adding anonymous /app/node_modules volume, `npm install` inside startup.sh leaves install incomplete and server crashes with missing modules.",
      "Replaced native bcrypt dependency with pure-JS bcryptjs in package.json and the three require() sites, to avoid node-gyp/native binary startup failures across Node versions.",
      "libxmljs was required only by the XML bulkProducts handler in core/appHandler.js; removing that native dependency and stubbing the handler avoids another Node 20 native build blocker."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
